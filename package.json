{
  "name": "zhiyu-tongluren",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server-hostinger.mjs",
    "start:local": "node server.mjs",
    "pages:deploy": "npx wrangler pages deploy public --project-name zhiyu-tongluren",
    "check": "node --check server-hostinger.mjs && node --check public/app.js && node --check public/generate.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=18" }
}
